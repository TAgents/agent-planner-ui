import React from 'react';

const DifferentiationSection: React.FC = () => (
  <section className="border-b border-border">
    <div className="mx-auto max-w-[880px] px-6 py-24 text-center sm:px-9">
      <span className="block font-mono text-[10px] uppercase tracking-[0.18em] text-amber">
        More than task tracking
      </span>
      <h2 className="mt-4 font-display text-[32px] font-medium leading-[1.18] tracking-[-0.03em] text-text sm:text-[36px]">
        A system for turning reusable operating knowledge into{' '}
        <span className="font-semibold italic text-amber">live, goal-aligned workspaces</span>{' '}
        — with agents in the work.
      </h2>
    </div>
  </section>
);

export default DifferentiationSection;
